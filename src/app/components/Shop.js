'use strict';

var React = require('react');

var Shop = React.createClass({
    render: function () {
        return (
            <div className="shop">
                Shop Page
            </div>
        );
    }
});

module.exports = Shop;